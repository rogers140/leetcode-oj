#include <iostream>
#include <vector>
#include <unordered_map>
using namespace std;

struct Point {
	int x;
	int y;
	Point() : x(0), y(0) {}
	Point(int a, int b) : x(a), y(b) {}
};

class Solution {
public:
	int maxPoints(vector<Point>& points) {
	// Line can be vertical, horizatal and diagonal with certain degrees 
	}
};
int main(int argc, char const *argv[])
{
	
	return 0;
}